# Code generated by ezactions. DO NOT EDIT.

name: buf-push
description: Push buf module updates to the Buf Schema Registry.
inputs:
  buf_token:
    required: true
    description: The Buf authentication token.
  default_branch:
    required: false
    description: The git branch that gets pushed to the main track on BSR.
    default: main
  github_token:
    required: false
    description: The Github authentication token.
    default: ${{ github.token }}
  input:
    required: false
    description: The Input path.
    default: .
  track:
    required: false
    description: The track to push to.
    default: ${{ github.ref_name }}
outputs:
  commit:
    description: The commit pushed to the Buf Schema Registry.
  commit_url:
    description: The URL to view commit on the Buf Schema Registry.
runs:
  image: Dockerfile
  using: docker
